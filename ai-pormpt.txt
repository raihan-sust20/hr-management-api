1. Pagination

Default page size (limit)? (e.g., 10, 20, 50) - 20
Max page size allowed? (e.g., 100) - 100
Default page number? (1) - 1
Response format for pagination metadata? - {
  page: number;
  limit: number;
  total: number;
  totalPages: number;
}

2. Filtering Options

Name search:

Case-insensitive ILIKE? (already mentioned) - yes
Partial match? (e.g., search "john" finds "John Doe", "Johnny") - yes


Other filters needed?:

Filter by designation? (e.g., designation=Engineer)
Filter by salary range? (e.g., min_salary=50000&max_salary=100000)
Filter by age range? (e.g., min_age=25&max_age=40)
Filter by hiring date range? (e.g., hiring_date_from=2023-01-01&hiring_date_to=2024-12-31)
Or just name search for now? - just name search for now



3. Sorting

Which fields can be sorted? (e.g., name, hiring_date, salary, age) - name
age
designation
hiring_date
date_of_birth
salary

Default sort? (e.g., created_at DESC, or name ASC) - created_at DESC
Support multiple sort fields? Or single field only? - single field only

4. Response Format

Return full employee objects with photo URLs? - no, only minimal data
Should photo URLs be included in list response? - no
Same format as POST/PUT responses? - yes

5. Query Parameters Structure
Example structure options:
Option A (Simple):
GET /employees?page=1&limit=20&name=john&sortBy=name&sortOrder=asc
Option B (More detailed):
GET /employees?page=1&limit=20&search=john&sort=name:asc,salary:desc
Which style do you prefer? - Option A
6. Authentication

Requires authentication? (I assume yes) - yes
Any restrictions on who can list employees? - no

7. Empty Results

Return empty array with pagination meta if no results? - yes
Or specific message?

8. Performance

Should we add database indexes for commonly filtered/sorted fields? - index already added
Any specific performance requirements? - no

9. Testing

Should I create unit and integration tests? (I assume yes) - yes
