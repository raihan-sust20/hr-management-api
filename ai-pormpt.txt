1. Update Behavior

Should all fields be required or can user update only specific fields (partial update)? - only specific fields can be updated
If partial update, which fields can be optional? - only these fields are allowed to be updated and can be optional: name
age
designation
hiring_date
date_of_birth
salary
photo_path


no other fields are allowed to update.

2. Photo Update

Is photo optional in the update request? - yes
If photo is NOT provided, should we:

Keep the existing photo?
Or require photo in every update?

- keep the existing photo

If new photo is uploaded, should we:

Delete the old photo file from disk?
Keep the old photo as backup?
- delete old photo file from disk


3. Date/Age Validation

If date_of_birth is updated, should we:

Recalculate age automatically? - yes
Validate new DOB against existing hiring_date? - yes


If hiring_date is updated, should we validate against existing date_of_birth? - yes
Can user update both dates in a single request? - yes

4. Business Rules

Can we update an employee that doesn't exist? (404 error?) - no
Should we validate that the new data still satisfies the 18+ at hiring rule? - yes
Any fields that should NOT be updatable (e.g., created_at, id)? - yes. Following fields
are not updatable: id
created_at
updated_at
.

5. Response Format

Return the complete updated employee object with photo URL? - yes
Same format as POST response? - yes

6. Authentication & Authorization

Requires authentication? (I assume yes) - yes
Any role-based restrictions? - no
Can any authenticated user update any employee? - yes

7. Testing

Should I create unit and integration tests?

Please provide your preferences, and I'll create the implementation plan!